[pytest]
testpaths = tests
addopts = -rsxX -l --tb=short -n 3 --dist=load
minversion = 6.0
norecursedirs = reports
markers =
    ui: marks UI tests
log_cli = True
log_cli_level = INFO
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S